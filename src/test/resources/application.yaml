spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/data_from_vk_test
    username: user
    password: user
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect